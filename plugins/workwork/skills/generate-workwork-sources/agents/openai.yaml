interface:
  display_name: "Generate WorkWork Sources"
  short_description: "Review suggested feeds and run the daily harvest"
  default_prompt: "Use $generate-workwork-sources to review suggested sources and run my daily looking-for harvest."
dependencies:
  tools:
    - type: "mcp"
      value: "workwork"
      description: "WorkWork source suggestion, acknowledgement, and harvest ingest tools"
      transport: "streamable_http"
      url: "https://api.workwork.bot/mcp"
